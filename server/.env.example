PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Oda durumu, kısa süreli oturum verisi ve denetim (audit) kaydı için Redis.
# `docker compose up -d` ile ayağa kaldırabilirsiniz (bkz. kök dizindeki docker-compose.yml).
REDIS_URL=redis://localhost:6379
